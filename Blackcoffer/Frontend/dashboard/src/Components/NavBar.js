import React from 'react';

const NavBar = () => {
  return (
    <div
      style={{
        width: '100%',
        textAlign: 'center',
        boxShadow: 'rgba(0, 0, 0, 0.35) 0px 5px 15px',
        fontSize: '50px',
        fontWeight: 'bold',
      }}
    >
      Dashboard
    </div>
  );
};

export default NavBar;
